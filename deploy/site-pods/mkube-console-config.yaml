# mkube-console config for rose1
# This is the raw config content; the ConfigMap in boot-order.yaml
# wraps this as data["config.yaml"].

listen_port: 9090
nodes:
  - name: rose1
    address: "http://192.168.200.2:8082"
registry:
  base_url: "http://192.168.200.2:5000"
logs_url: "http://logging.kube.gt.lo:8084"
